






<input type="checkbox" id= "add_number" name="menu" />
<input type="checkbox" id= "capitalize_first_letter" name="menu"/>

<input type="checkbox" id="add_special_cha" name="add special cha" data-menu="add special cha"
style="opacity: 0; pointer-events: none;position: absolute"/>

<div class="tgl-group " >
<label aria-label="special character" for="add_special_cha" data-menu-label="menu" class="toggle-text">
!@#$%&...</label>

</div>

<style>

	.tgl-group {
	position: relative;
	display: flex;
	justify-content: center;
	align-items: center;
	height: var(--portion);
}

	.toggle-text {
	position: relative;
  background: none ;
	font-size: var(--regular-font);
	padding: var(--default-padding);
	border-radius: var(--radiu-a);	
	border: 1px solid  var(--focus-color);
  user-select: none; 
  cursor: pointer;
  color: var(--focus-color);
  background-color: var(--toggle-bg);
  opacity: 1;
	box-shadow: 1px 3px 0px var(--toggle-bg-darker) ;

}

.toggle-text::before{
  content: "";
	position: absolute;
	inset: 0;
	z-index: -1;		
	background-color: var(--toggle-bg-darker);
	scale: 0.65;
  border-radius: 3ch;
	filter: blur(10px);
	mix-blend-mode: soft-light;
}



.toggle-on {
	position: relative;
	border: 1px solid transparent;
  background-color: var(--primery-transparent);
  color: var(--accent-color);
/* 		filter: drop-shadow(1px 3.5px 0px var(--dark-shadow) ); */
	box-shadow: 1px 3px 0px var(--dark-shadow) ;


  }

.toggle-on::before{
  content: "";
	position: absolute;
	inset: 0;
	z-index: -1;		
	background-color: var(--glow);
  transform: scaleX(1.2);
  border-radius: 3ch;
	filter: blur(10px);

}

.tgl-group > * {

	border: none;
	border-radius: 0px;

}


.tgl-group > :first-child {
	border-top-left-radius: var(--radiu-a);
	border-bottom-left-radius: var(--radiu-a);


}

.tgl-group > :last-child {
	border-top-right-radius: var(--radiu-a);
	border-bottom-right-radius: var(--radiu-a);

}

.tgl-group > *::after{
	border: none;
	content: "";
	position: absolute;
	border-top:0.2px solid var(--light-reflection);
	border-bottom:0.2px solid var(--light-reflection);
	box-shadow:  inset 0px 0.4px 0px 0px var(--light-reflection);
	border-radius: 0px;
	inset: 0;
	mix-blend-mode: soft-light;

}

.tgl-group > :first-child::after{
	content: "";
	position: absolute;
	border-top-left-radius: var(--radiu-a);
	border-bottom-left-radius: var(--radiu-a);
	border-left: 0.2px solid var(--light-reflection) ;
	box-shadow:   inset 0.6px 0.4px 0px 0px var(--light-reflection);

}

.tgl-group > :last-child::after{
	content: "";
	position: absolute;
	border-top-right-radius: var(--radiu-a);
	border-bottom-right-radius: var(--radiu-a);
	border-right: 0.2px solid var(--light-reflection) ;
	box-shadow:   inset 0px 0.4px 0px 0px var(--light-reflection) ;

}

	
</style>
