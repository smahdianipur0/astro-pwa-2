<div style="
	position: relative;
	display: flex;
	justify-content: center;
	align-items: center;" >

<label  id= "l_capitalize_first_letter" aria-label="Capitalize First Letter" for="capitalize_first_letter" 
data-menu-label="capitalize" class="toggle-button first">
Capitalize</label>

<input type="checkbox" id= "capitalize_first_letter" name="pass-ops" data-menu="capitalize"
class="hide-checkbox" checked/>

<label id= "l_add_number" aria-label="Add Number" for="add_number" 
data-menu-label="add_number" class="toggle-button btn-middle">
Number</label>

<input type="checkbox" id= "add_number" name="pass-ops"
class="hide-checkbox" checked />

<label id="l_add_special_cha" aria-label="Special Character" for="add_special_cha" 
data-menu-label="add_special_cha" class="toggle-button last">
!@#$%&...</label>

<input type="checkbox" id="add_special_cha" name="pass-ops" data-menu="add special cha"
class="hide-checkbox" checked/>

</div>

<style is:global>
 
.toggle-button {
	position: relative;
	padding:var(--default-padding);
	line-height: var(--gap-x04);
	user-select:none;
	cursor:pointer;
	border-radius:var(--radius-a);
	background-color: var(--primery-transparent);
  color: oklch(var(--gray-95) / 1);
	box-shadow: 1px 2.5px 0px var(--primary-shadow) ;
}

:root:not(:has(#capitalize_first_letter:checked)) #l_capitalize_first_letter,
:root:not(:has(#add_number:checked)) #l_add_number ,
:root:not(:has(#add_special_cha:checked)) #l_add_special_cha,
.toggle-button:disabled{
	background-color: transparent;
	color:oklch(var(--gray-95) / 0.5);
	box-shadow:1px 2.5px 0px oklch(var(--gray-15) / 0.6);
}

.toggle-button::before{
	content: "";
	position: absolute;
	inset: 0;
	z-index: var(--z-layer-middle);		
	background-color: var(--glow);
  scale: 0.65;
  border-radius: 3ch;
	filter: blur(10px);
}

:root:not(:has(#capitalize_first_letter:checked)) #l_capitalize_first_letter::before ,
:root:not(:has(#add_number:checked))  #l_add_number::before ,
:root:not(:has(#add_special_cha:checked)) #l_add_special_cha::before ,
.toggle-button:disabled::before {
	content: "";
	position: absolute;
	inset: 0;
	z-index: var(--z-layer-beneath);
	transform: scaleX(1.2);	
  border-radius: 3ch;
	filter: blur(10px);
	mix-blend-mode: screen ;

}

.toggle-button::after{
	content: "";
	border: none;
	z-index: var(--z-layer-beneath);
	background: oklch(var(--gray-15) / 1);
	position: absolute;
	border:0.2px solid var(--rim-light);
	box-shadow:  inset 0px 0.4px 0px 0px var(--rim-light);
	border-radius: var(--radius-a);
	inset: 0;
}



.first{
	border-top-right-radius: 0;
	border-bottom-right-radius: 0;
}

.first::after{
	content: "";
	border-top-right-radius: 0;
	border-bottom-right-radius: 0;
	border-right: 0 ;
}

.last{
	border-top-left-radius: 0;
	border-bottom-left-radius: 0;
}

.last::after{
	content: "";
	border-top-left-radius: 0;
	border-bottom-left-radius: 0;
	border-left: 0 ;
}

.btn-middle{
	border-radius: 0;
}

.btn-middle::after{
	border-radius: 0;
	border-right: 0 ;
	border-left: 0 ;
}


:root:has(#capitalize_first_letter:focus-visible) #l_capitalize_first_letter,
:root:has(#add_number:focus-visible)  #l_add_number ,
:root:has(#add_special_cha:focus-visible) #l_add_special_cha {
	outline: Highlight auto 1px;
  outline: -webkit-focus-ring-color auto 1px;
}

.toggle-button:active::before {
  content: "";
  position: absolute;
  inset: 0;
  z-index: -1;    
  scale: 0.65;
  border-radius: 3ch;
  filter: blur(10px);
  mix-blend-mode: soft-light;
}



</style>