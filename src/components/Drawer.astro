<nav class="drw-list" id="route">

<input type="radio" name="tab" class="sub-route" id="one" checked />
<input type="radio" name="tab" class="sub-route" id="two" />

<input aria-label="theme toggle" class="sub-route" id="theme-toggle"  type="checkbox" id="theme-toggle"
name="theme" style="margin-bottom: 8ch; margin-top: auto;"> 

</nav>



<style is:global>

.sub-route {
  appearance: none;
  -webkit-appearance: none;
  border: none;
  cursor: pointer;
  font-size:var(--regular-font);
  background-color: transparent;
  text-align: start;
  font-size: var(--regular-font);
  text-align: start;
  line-height: 2ch;
  transition-duration: 0.2s;
}

#one:after {
  content: 'Random';
  color:var(--accent-color); 
}

#two:after {
  content: 'Manual';
  color:var(--accent-color); 
}

#theme-toggle:before {
  color:var(--accent-color);
  content: 'Change Theme: ☀️'; 
}

:root:has(#theme-toggle:checked) #theme-toggle:before {
  content: 'Change Theme: 🌙'; 
}


a {
  text-decoration: none; 
  height: 25px;
  margin:10px var(--portion); 
  margin: 100px;
}

.drw-list {
  display: grid;
  grid-template-columns: auto;
  grid-gap: 1ch;
  opacity: 0.1;
  transform: translateX(-50px);
}


:root:has(#menu:checked) .drw-list {
    transform: translateX(calc((var(--portion) - var(--small-clickable)) / 4 ));
    opacity: 1;
    transition-delay: 0.2s;
    transition-duration: 0.2s;

    @starting-style{
    opacity: 0.1;
    transform: translateX(-50px);    
    }
}

.tab-content {
    display: none;
    width: 100%;
}

:root:has(#one:checked) .tab-content.one {
    display: block;
}


:root:has(#two:checked) .tab-content.two {
    display: block;
}





</style>

<script>

const checkbox = document.getElementById("menu")! as HTMLInputElement;

document
    .getElementById("one")!
    .addEventListener("click", (event) => toggleCheckbox(event));
document
    .getElementById("two")!
    .addEventListener("click", (event) => toggleCheckbox(event));

function toggleCheckbox(event: MouseEvent) {
    if (event.detail === 1) {
        checkbox.checked = !checkbox.checked;
    }
}

document.getElementById("route")!.addEventListener("keydown", toggleOnEnter);

function toggleOnEnter(event: KeyboardEvent) {
    if (event.key === "Enter") {
        if (event.detail === 1) {
            checkbox.checked = !checkbox.checked;
        } else {
            checkbox.checked = !checkbox.checked;
        }
    }
}



</script>