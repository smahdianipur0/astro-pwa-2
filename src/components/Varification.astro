<input id="use_varif" type="checkbox" style="accent-color: var(--primary);">

<div style="display: flex;
position: relative;
justify-content: space-between;
align-items: center;
gap: 1ch;">

	<div  id="varif_detail_id" class= "varif_detail_cl" style="line-height: 2ch;
    font-weight: 300;
    font-size:var(--big-font);"> varify</div>

	<button  id = "varif_detail_re" class=" unstyle-button copy varif_detail_cl">
	hi </button>

</div>

<p id="varif_hint" class="hint varif_detail_cl">hello</p>


<style >
	
#use_varif::after{
	content :  "Use Decrypted text as key for varification code" ;
	display: block;
	margin-left: 3ch;
	color: var(--accent-color);
	font-size: var(--regular-font);
	width: 41ch;
	line-height: 2ch;
}

:root:has(#enc:checked) #use_varif::after {
	content :  "Use plain text as key for varification code" ;

}
.varif_detail_cl{
	display: none;
}

:root:has(#use_varif:checked) .varif_detail_cl{
	display: block;
}

</style>