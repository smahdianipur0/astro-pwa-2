---
import Tabs from '../components/Tabs.astro';
---

<div class="card-bg" style="height: 18.4ch;"></div> 
<form autocomplete="off">
	
	<div class="relative ">
		<input id="key" type="text" class="k glass dent-shadow " name="Key" placeholder="Key">
		<span class="key-indicator"></span>
	</div>
	
	<div class="relative" style ="margin-bottom: 2ch;"> 
		<input id="iv" type="text" class="v glass dent-shadow " name="IV" placeholder="IV">
		<span class="key-indicator"></span>
	</div>

</form>

<Tabs name="encdec" tab1="enc" tab2="dec" content1="cenc" content2="cdec">
	<fragment slot="tab1">

		<label >
		<input id="auto_pass" type="checkbox" style="margin-bottom: 2ch;
		margin-top: 2ch;
		accent-color: var(--primary);"> Use Password as Plain Text </label>
		<textarea id="plain_text" class="txt-area glass dent-shadow" rows="2"placeholder="Plain Text"> </textarea>

		<p style="margin-top: 3ch; margin-bottom: 1ch; font-size: var(--extra-small-font); opacity: 0.6;">Encrypted Text : </p>

		<textarea id="result_e"  class="txt-area" style="border: 0px solid transparent;" rows="2" readonly> </textarea>

		<div class="btn-group"> 
					
			<button class="toggle-text first" style="height:4ch;
			background-color: var(--primery-transparent);
  		color: var(--accent-color);
			box-shadow: 1px 3px 0px var(--dark-shadow) ;">Copy</button>



			<input type="checkbox" id= "copy_e"  data-menu="copy_e"
			style="opacity: 0; pointer-events: none;position: absolute" checked/>

			<label aria-label="Show QR Code" for="copy_e" 
			data-menu-label="show_qr_code" class="toggle-text last last_e" 
			style="width: 17ch; height:4ch ;
			background-color: var(--primery-transparent);
  		color: var(--accent-color);
			box-shadow: 1px 3px 0px var(--dark-shadow) ; "> &nbsp </label>

			<input type="checkbox" id= "show_qr_code" name="show_qr_code" data-menu="show_qr_code"
			style="opacity: 0; pointer-events: none;position: absolute" checked/> 

		</div>

	</fragment>


	<fragment slot="tab2">

		<textarea  class="txt-area glass dent-shadow" rows="2"placeholder="Plain Text"
		style="margin-top:2ch"> </textarea>

		<p style="margin-top: 3ch; margin-bottom: 1ch; font-size: var(--extra-small-font); opacity: 0.6;">Decrypted Text : </p>

		<textarea  class="txt-area" style="border: 0px solid transparent;" rows="2" readonly> </textarea>

		<div class="btn-group"> 
					
			<button class="toggle-text first" style="height:4ch;
			background-color: var(--primery-transparent);
  		color: var(--accent-color);
			box-shadow: 1px 3px 0px var(--dark-shadow) ;">Copy</button>



			<input type="checkbox" id= "copy_d"  data-menu="copy_d"
			style="opacity: 0; pointer-events: none;position: absolute" checked/>

			<label aria-label="Show QR Code" for="copy_d" 
			data-menu-label="show_qr_code" class="toggle-text last last_d" 
			style="width: 17ch; height:4ch ;
			background-color: var(--primery-transparent);
  		color: var(--accent-color);
			box-shadow: 1px 3px 0px var(--dark-shadow) ; "> &nbsp </label>

			<input type="checkbox" id= "show_qr_code" name="show_qr_code" data-menu="show_qr_code"
			style="opacity: 0; pointer-events: none;position: absolute" checked/> 

		</div>

	</fragment>
</Tabs>



<style is:global>


#enc:after { content: 'Encrypt';}
#dec:after { content: 'Decrypt';}

:root:has(#enc:checked) { 
	#cenc{display: block;} 
	#encdec {transform: translate3d(-5ch, 0, 0);}
}

:root:has(#dec:checked) {
 #cdec {display: block;} 
 	#encdec { transform: translate3d(5ch, 0, 0);}
}



.k{
  font-size: var(--regular-font);
  width: 100%;
  display: inline-block;
  color: var(--accent-color);

	background: var(--inout-bg);
	border: none;
	border-radius: var(--radiu-a);
	border-bottom-left-radius: 0;
	border-bottom-right-radius: 0;
	padding: 0.95ch 1.5ch;
	margin-bottom: 0;
	border-bottom: 0.1px solid var(--primary);
}

.v{
	font-size: var(--regular-font);
  width: 100%;
  display: inline-block;
  color: var(--accent-color);

	background: var(--inout-bg);
	border: none;
	border-radius: var(--radiu-a);
	border-top-left-radius: 0;
	border-top-right-radius: 0;
	padding: 0.95ch 1.5ch;
}
 
.k:focus,
.v:focus{
	  background-color:var(--select-color) ;
	  outline: none;
  	background: var(--input-bg-focus);
}

.txt-area{
  background-color: transparent; 
  font-size: var(--regular-font);
  font-family: inherit;
  width: 100%;
  padding: var(--default-padding);
  display: inline-block;
  border: 1px solid transparent;
  border-radius: var(--radiu-a);
  resize: vertical;
  border-bottom: 1px solid var(--primary);
  border-bottom-left-radius: 0px;
  border-bottom-right-radius: 0px;
  color: var(--accent-color);
}


.txt-area:focus {
	border: 1px solid var(--primary);
	border-radius: var(--radiu-a);
	outline: none;
}


.btn-group {
	display: flex;
	justify-content: center;
	align-items: center;
	height: var(--portion);
}





.lasta{
/* 	padding: var(--default-padding);
	border-top-left-radius: 0;
	border-bottom-left-radius: 0;
	font-size: var(--regular-font); */
}

.last_e::after{
	content: "Show QR Code"; 
	text-align: center;
	inset: 0;
	
	padding:var(--default-padding);	

}

:root:has(#copy_e:checked) .last_e::after{
	content: "Hide QR Code"; 
}

.last_d::after{
	content: "Show QR Code"; 
	text-align: center;
	inset: 0;
	
	padding:var(--default-padding);	

}

:root:has(#copy_d:checked) .last_d::after{
	content: "Hide QR Code"; 
}


:root:has(#copy_d:focus-visible) .last_d,
:root:has(#copy_e:focus-visible) .last_e {
	outline: Highlight auto 1px;
  outline: -webkit-focus-ring-color auto 1px;
}
	
</style>